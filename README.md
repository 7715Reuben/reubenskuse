# reubenskuse

Personal website for **reubenskuse.com**.

## Run locally

```bash
python3 -m http.server 8080
```

Then open http://localhost:8080 in your browser.

## Notes

- Replace `assets/reuben-portrait.svg` with your real portrait image when ready.
- The page background shifts from lighter blue to deeper blue as the user scrolls.
